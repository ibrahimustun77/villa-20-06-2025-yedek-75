
import React from 'react';
import VillaDetailsContainer from './VillaDetailsContainer';

const VillaDetails = ({ villaData }) => {
  return <VillaDetailsContainer villaData={villaData} />;
};

export default VillaDetails;
